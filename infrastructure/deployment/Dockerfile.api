# Dockerfile for GS Gacuba Backend API
FROM node:20-alpine AS runner

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY apps/api ./apps/api
COPY . .

EXPOSE 3001
ENV NODE_ENV=production
ENV PORT=3001

CMD ["node", "apps/api/src/server.ts"]
